name: Build and Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-20.04, windows-latest]

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install System Dependencies (Linux only)
      if: runner.os == 'Linux'
      run: |
        sudo apt-get update
        sudo apt-get install -y libasound2-dev

    - name: Install Python Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pyinstaller

    - name: Build with PyInstaller (Windows)
      if: runner.os == 'Windows'
      run: pyinstaller --windowed --name audiobook-app-windows --collect-all piper --collect-all espeakng_loader Audiobook/application.py

    - name: Zip the Windows Build
      if: runner.os == 'Windows'
      run: Compress-Archive -Path dist/audiobook-app-windows/* -DestinationPath dist/audiobook-app-windows.zip

    - name: Build with PyInstaller (Linux)
      if: runner.os == 'Linux'
      run: pyinstaller --windowed --name audiobook-app-linux --collect-all piper --collect-all espeakng_loader Audiobook/application.py

    - name: Zip the Linux Build
      if: runner.os == 'Linux'
      run: |
        cd dist
        zip -r audiobook-app-linux.zip audiobook-app-linux/

    - name: Upload to GitHub Release
      uses: softprops/action-gh-release@v2
      with:
        files: |
          dist/audiobook-app-windows.zip
          dist/audiobook-app-linux.zip
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}